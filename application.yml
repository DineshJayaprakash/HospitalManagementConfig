spring:
  cloud:
      consul:
           host: localhost
           port: 8500
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/usermodule?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: Balaji@407
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
             hibernate:
                     dialect: org.hibernate.dialect.MySQL8Dialect 
                     
 ## note while using docker change it host.docker.internal
 ## username: ${username}
 ## password: ${password}


 
